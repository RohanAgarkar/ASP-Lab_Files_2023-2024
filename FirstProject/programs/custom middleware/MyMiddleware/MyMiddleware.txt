namespace FIRSTPROJECT.MyMiddleware
{
    public class MyMiddleware : IMiddleware
    {
        public async Task InvokeAsync(HttpContext context, RequestDelegate next){
            await context.Response.WriteAsync("first content part");
            await next(context);
            await context.Response.WriteAsync("Second content part");
        }
    }
}